import functions

def main():
    #print("I am in main")
    #print("my name is", __name__)
    #functions.welcome_message() # calls function welcome message
    #functions.welcome_message2("Vilmor") # calls function welcome message 2 
    #number = functions.get_random_num()
    #print(number)

    #user_input = int(input("Please enter the length of your side in meteres: "))
    #area_square = functions.calculate_area_square(user_input)
    #print("The area of a square with a side length of 10 is", area_square)

    prod_desc = functions.get_product_description()
    amount = functions.get_amount()
    price = functions.get_price()
    total_cost = functions. calc_total

if __name__ == "__main__": # function that calls and run main
    main() 


